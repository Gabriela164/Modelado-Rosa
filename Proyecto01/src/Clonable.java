/**
* Interface que le permite clonarse a las instancias de las clases que la implementen
* @author Immerwahr, Gaby, SanMa
* @version 1.2
**/
public interface Clonable{

	/**
	 * Declaración del método que clona una instacia de la clase
	 * @return el nuevo objeto clonado
	 */
	public Object clonar();
}